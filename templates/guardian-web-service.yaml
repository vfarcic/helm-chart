apiVersion: v1
kind: Service
metadata:
  name: {{ include "shipa.fullname" . }}-guardian
  labels: {{- include "shipa.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    name: {{ include "shipa.fullname" . }}-guardian
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: web
    - port: 22
      targetPort: 22
      protocol: TCP
      name: ssh
